// If only there was an operator for that! <--- see what I did here?
//
// Not sure if the worst thing is that I am writing this, or that Copilot
// actually suggested this implementation.
pub fn invert(b: bool) -> bool {
    if b == true {
        false
    } else {
        true
    }
}
